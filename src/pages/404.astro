---
import Layout from '../layouts/Layout.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
---

<Layout 
  title="Seite nicht gefunden - LMP Consulting GmbH" 
  description="Die angeforderte Seite konnte nicht gefunden werden."
  noindex={true}
>
  <Header />
  <main class="pt-16 lg:pt-20">
    <section class="section min-h-screen flex items-center">
      <div class="container">
        <div class="max-w-2xl mx-auto text-center">
          <!-- 404 Illustration -->
          <div class="mb-8">
            <div class="text-8xl lg:text-9xl font-bold text-primary/20 mb-4">404</div>
            <div class="text-primary mb-6">
              <svg class="w-24 h-24 mx-auto" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1" d="M9.172 16.172a4 4 0 015.656 0M9 12h6m-6-4h6m2 5.291A7.962 7.962 0 0112 15c-2.34 0-4.29-1.009-5.824-2.563M15 9.34c-1.292-1.292-3.292-1.292-4.584 0M21 12a9 9 0 11-18 0 9 9 0 0118 0z" />
              </svg>
            </div>
          </div>

          <h1 class="heading-2 mb-4">
            Seite nicht gefunden
          </h1>
          
          <p class="text-large mb-8 text-secondary-600">
            Die angeforderte Seite konnte leider nicht gefunden werden. 
            Möglicherweise wurde sie verschoben oder existiert nicht mehr.
          </p>

          <!-- Action Buttons -->
          <div class="flex flex-col sm:flex-row gap-4 justify-center mb-12">
            <a href="/" class="btn-primary">
              Zur Startseite
            </a>
            <button 
              onclick="history.back()" 
              class="btn-secondary"
            >
              Zurück
            </button>
          </div>

          <!-- Helpful Links -->
          <div class="card">
            <h2 class="text-xl font-semibold mb-4">Vielleicht suchen Sie nach:</h2>
            <div class="grid grid-cols-1 sm:grid-cols-2 gap-4 text-left">
              <a href="/leistungen" class="flex items-center p-3 rounded-lg hover:bg-secondary-50 transition-colors duration-300 group">
                <div class="text-primary mr-3 group-hover:scale-110 transition-transform duration-300">
                  <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9.663 17h4.673M12 3v1m6.364 1.636l-.707.707M21 12h-1M4 12H3m3.343-5.657l-.707-.707m2.828 9.9a5 5 0 117.072 0l-.548.547A3.374 3.374 0 0014 18.469V19a2 2 0 11-4 0v-.531c0-.895-.356-1.754-.988-2.386l-.548-.547z" />
                  </svg>
                </div>
                <div>
                  <div class="font-medium text-secondary-900">Unsere Leistungen</div>
                  <div class="text-sm text-secondary-600">IT-Beratung & Projektleitung</div>
                </div>
              </a>

              <a href="/projekte" class="flex items-center p-3 rounded-lg hover:bg-secondary-50 transition-colors duration-300 group">
                <div class="text-primary mr-3 group-hover:scale-110 transition-transform duration-300">
                  <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 11H5m14 0a2 2 0 012 2v6a2 2 0 01-2 2H5a2 2 0 01-2-2v-6a2 2 0 012-2m14 0V9a2 2 0 00-2-2M5 11V9a2 2 0 012-2m0 0V5a2 2 0 012-2h6a2 2 0 012 2v2M7 7h10" />
                  </svg>
                </div>
                <div>
                  <div class="font-medium text-secondary-900">Referenzen</div>
                  <div class="text-sm text-secondary-600">Erfolgreiche Projekte</div>
                </div>
              </a>

              <a href="/profil" class="flex items-center p-3 rounded-lg hover:bg-secondary-50 transition-colors duration-300 group">
                <div class="text-primary mr-3 group-hover:scale-110 transition-transform duration-300">
                  <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z" />
                  </svg>
                </div>
                <div>
                  <div class="font-medium text-secondary-900">Über uns</div>
                  <div class="text-sm text-secondary-600">Profil & Expertise</div>
                </div>
              </a>

              <a href="/kontakt" class="flex items-center p-3 rounded-lg hover:bg-secondary-50 transition-colors duration-300 group">
                <div class="text-primary mr-3 group-hover:scale-110 transition-transform duration-300">
                  <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 8l7.89 4.26a2 2 0 002.22 0L21 8M5 19h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z" />
                  </svg>
                </div>
                <div>
                  <div class="font-medium text-secondary-900">Kontakt</div>
                  <div class="text-sm text-secondary-600">Nachricht senden</div>
                </div>
              </a>
            </div>
          </div>

          <!-- Search -->
          <div class="mt-8">
            <div class="max-w-md mx-auto">
              <label for="search" class="sr-only">Suche</label>
              <div class="relative">
                <input
                  type="text"
                  id="search"
                  placeholder="Suche auf der Website..."
                  class="w-full px-4 py-3 pl-12 border border-secondary-300 rounded-lg focus:ring-2 focus:ring-primary focus:border-transparent"
                />
                <div class="absolute inset-y-0 left-0 pl-4 flex items-center pointer-events-none">
                  <svg class="w-5 h-5 text-secondary-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z" />
                  </svg>
                </div>
              </div>
            </div>
          </div>
        </div>
      </div>
    </section>
  </main>
  <Footer />

  <script>
    // Simple search functionality
    const searchInput = document.getElementById('search');
    if (searchInput) {
      searchInput.addEventListener('keypress', (e) => {
        if (e.key === 'Enter') {
          const query = searchInput.value.trim().toLowerCase();
          
          // Simple keyword-based navigation
          if (query.includes('leistung') || query.includes('service') || query.includes('beratung')) {
            window.location.href = '/leistungen';
          } else if (query.includes('projekt') || query.includes('referenz') || query.includes('portfolio')) {
            window.location.href = '/projekte';
          } else if (query.includes('profil') || query.includes('über') || query.includes('team')) {
            window.location.href = '/profil';
          } else if (query.includes('kontakt') || query.includes('email') || query.includes('telefon')) {
            window.location.href = '/kontakt';
          } else if (query.includes('impressum')) {
            window.location.href = '/impressum';
          } else if (query.includes('datenschutz') || query.includes('dsgvo')) {
            window.location.href = '/datenschutz';
          } else {
            // Default to homepage for any other search
            window.location.href = '/';
          }
        }
      });
    }
  </script>
</Layout>